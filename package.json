{
  "name": "react-portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd client && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm install vite && npm run build"
  },
  "author": "Aidan Reed",
  "license": "MIT",
  "dependencies": {
    "tailwindcss": "^3.4.1"
  }
}
